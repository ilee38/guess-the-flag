import React, {Component} from 'react';
import './Navbar.css';

const Navbar = (props) => {
    return(<div class="navbar">
        <h1>Guess The Flag</h1>
      </div>
    );
}

export default Navbar;